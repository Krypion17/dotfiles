(defpoll battery
  :interval "1s"
  "./scripts/battery.sh")

(defpoll iconbat
  :interval "1s"
  "./scripts/battery.sh --icon")

(defpoll colorbat
  :interval "1s"
  "./scripts/battery.sh --color")

(defwidget batterywid []
  (box
    :class "widgets"
    :halign "end"
    (label
      :class "icon"
      :style "color: ${colorbat};"
      :text "${iconbat}")
    "${battery}% "))
