(defvar volume
  "30")

(defvar iconvol
  "奄")

(defvar classvol
  "clickable")

(defwidget volumewid []
  (button
    :class "${classvol}"
    :style "padding: 0;"
    :onclick "./scripts/css/click.sh vol"
    (box
      :class "widgets"
      :style "background-color: rgba(0, 0, 0, 0); margin: 0;"
      :space-evenly false
      :spacing 10
      (label
        :class "icon"
        :text "${iconvol}")
      "${volume}%")))
