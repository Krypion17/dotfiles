(defpoll title
  :interval "1s"
  :initial "Home"
  "./scripts/widgets/title.sh")

(defwidget titlewid []
  (box
    :class "title"
    :space-evenly false
    (label
      :limit-width 100
      :text "${title}")))
